# 2026-09-25 — Prepare Pepsy 0.5.0

- Scope: prepare release metadata and validate distributions; the user selected
  version 0.5.0 and retained the direct `develop` / `main` workflow.
- Branch / baseline: `develop` / `3b9796c`, initially shared with `main`.
- Commit status: this handoff accompanies the release-preparation commit.

## Changes

- Set package metadata and README to 0.5.0; move the accumulated changelog
  into the dated 0.5.0 section and keep an empty Unreleased section.
- Name the 0.4.1-to-0.5.0 migration and clarify the pre-1.0 policy: minor
  releases may contain documented incompatible changes, while patches remain
  compatible. Existing named import aliases retain their 0.x window.
- No numerical implementation, dependency requirement, or test changes.

## Validation

- Built `pepsy-0.5.0.tar.gz` and `pepsy-0.5.0-py3-none-any.whl` using the
  selected Python environment and temporary build tools. The default build
  creates the wheel from the sdist; `twine check --strict` passed for both.
- Verified all 187 Python modules in the wheel match the checkout, plus the
  license, typing marker, version, Python requirement, and absence of caches.
  The source archive excludes device-local policy and Git internals.
- Installed the wheel into a temporary target. Dependency-free import,
  runtime/distribution version agreement, lazy experimental discovery, and
  a two-qubit gate/norm smoke check passed using that installed wheel.
- API/package suites against the installed wheel: **58 passed**, with eight
  expected alias deprecation warnings. The first checkout run had one failure
  because the shared environment still advertises installed version 0.4.1;
  using the freshly installed artifact resolved it without changing tests or
  the shared environment.
- Strict Sphinx HTML build: exit 0, no diagnostics. Local documentation links
  and `git diff --check` passed.

## Release state

- Artifacts are local under `/tmp/pepsy-0.5.0-dist`; temporary installations
  and build tools leave the shared environment unchanged.
- At preparation time, baseline CI runs 36164831561 (`main`) and
  36164827766 (`develop`) remain in progress. On main, seven jobs have passed;
  only the extended suite is pending. Earlier green runs are documented in
  the previous handoffs and do not substitute for release-commit CI.
- Publish this preparation directly to both branches without a PR. Tagging
  and registry publication are not represented as completed by this commit.
  Check release-commit CI before a release tag. The existing release workflow
  builds on `v*` tags; registry publication uses explicit workflow-dispatch
  inputs and its configured environment.
